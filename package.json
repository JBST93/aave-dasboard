{
  "name": "defi-dashboard",
  "version": "1.0.0",
  "scripts": {
    "start": "gunicorn app:app",
    "postinstall": "cd frontend && npm install && npm run build"
  },
  "engines": {
    "node": ">=14.x"
  }
}
