{
  "name": "defi-dashboard",
  "version": "1.0.0",
  "scripts": {
    "start": "gunicorn app:app",
    "postinstall": "npm install --prefix frontend && npm run build --prefix frontend"
  },
  "engines": {
    "node": ">=14.x"
  }
}
